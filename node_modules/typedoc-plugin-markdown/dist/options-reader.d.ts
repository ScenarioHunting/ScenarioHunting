import { Options, OptionsReader } from 'typedoc';
import { Logger } from 'typedoc/dist/lib/utils';
export declare class CustomOptionsReader implements OptionsReader {
    priority: number;
    name: string;
    read(container: Options, logger: Logger): void;
    getOptionsFile(optionsFile: string): any;
}
